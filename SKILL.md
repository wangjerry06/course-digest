---
name: course-digest
description: 课件总结：把 PDF 课件压成带页锚点的 Markdown 总结，并在本地浏览器里与 PDF 并排查看、点击段落跳页。仅当用户显式点名「总结课件 / course-digest / 用 course-digest 总结」时启用；未点名不介入。
---

# course-digest — 课件总结

把一份 PDF 课件变成「带页锚点的总结」：算法先删掉渐进重复页，agent 只读压缩后的正文写总结，
最后在本地浏览器里与 PDF 并排查看、点击段落跳到对应页。完整版 PDF 始终保留。

## 触发门禁（先读这条）

**只在用户显式点名时执行**：说「用 course-digest 总结这份课件」「总结一下这个课件」这类明确指令。

- 用户只是聊到课件、发来 PDF 但没说要总结 → **不要执行**，问一句再动
- 同一份课件第二次要总结 → 先 `list` 找 docId，走 `open` 复现，**不要重新生成**

## 前置

- macOS（MVP 只保证 macOS）；Python 3.10+（`str | None` 注解在 3.10 才允许运行时求值）
- **零额外依赖**：`pypdf` 与前端库（PDF.js / marked / KaTeX / highlight.js）都已 vendor 在本仓库
- 下文命令都在**本 skill 的根目录（即本 `SKILL.md` 所在目录）**执行；
  `python3 -m course_digest ...` 指的是这个目录下的包，不在这个目录跑会报 `No module named course_digest`

## 四步执行链

### 0. 先读用户偏好

读 `~/.course-digest/style.md`（总结风格：语言 / 篇幅 / 实例密度 / 公式 / 复习导向）。
- 文件不存在 → 按默认：中文、标准篇幅、实例多、公式解释符号、复习导向
- 用户**这次**临时说「短一点」→ 只对这次生效，**不要写回文件**
- 用户说「**以后都**这样」「记住这个风格」→ 改该文件的「当前偏好」段
- 用户说「恢复默认」→ 把「当前偏好」段写回默认值
- 改完文件后，用一句话告诉用户「已记下：×××」

### 1. 纳入文档库

```bash
python3 -m course_digest import "<PDF 路径>"      # stdout 就是 docId，记下来
```

### 2. 提文本 + 压缩

```bash
python3 -m course_digest extract <docId>
```

读 stdout 的 stats：`groups` / `kept_pages` / `chars_before` → `chars_after`（压缩比）/ `split_by_guard`。

- `split_by_guard > 0` → stderr 会列出「差点被误删、被安全网拆回来的页号」，值得人看一眼
- **不要把整本 PDF 或整份 `extract.json` 喂给自己** —— 压缩的意义就是省这一步

### 3. 读压缩后的正文，写 `summary.md`

```bash
python3 -m course_digest text <docId>             # 只输出保留页正文，带 ===== P<n> ===== 标记
python3 -m course_digest text <docId> --pages 1-30   # 文档很长时分段读
```

- 正文按主题分段写总结，**每段前加一行锚点注释**（格式见下）
- 文本层太薄（每页 < ~200 字符、图/公式密集）→ 按 `references/visual-patch.md` 做**视觉补丁**，并在总结里注明哪几页补过
- 产出写到任意临时路径即可，第 4 步再交给 `publish`

### 4. 发布

```bash
python3 -m course_digest publish <docId> --summary <md路径> [--drop "3,7-9"]
```

`publish` 会：校验锚点 → 生成简化版 PDF 与页码映射（**原 PDF 不动**）→ 落盘 summary/meta →
起本地服务 → 打开页面，并把 URL 打到 stdout。

- `--drop` 只用于**你判定与主题无关**的页（有损操作，宁缺毋滥）
- 看 stderr 的**锚点覆盖率**：目标是 **100%**；< 50% 会告警
- **覆盖率不满 100% 时不用猜**：stderr 会把没锚点的段落逐条列出来（行号 + 前 30 字符的预览），
  按提示**逐个补锚点后重发**即可 —— 一轮修完，**不要整份重写**（白费 token）
- `publish` 会在落盘的 `summary.md` **尾部追加「回程票」**（一行 HTML 注释写 docId +
  一段 `<sub>` 说明怎么重开）。这是工具管理的元数据：**你不要把 footer 当正文去改或删**，
  重发时会自动重建（docId 以命令行为准）
- 把 URL 和压缩比（页数、字符）一并报给用户
- 页面右上角有「下载 MD」「下载 PDF」两个按钮：前者下载总结（尾部带上面那张回程票），
  后者下载简化版 PDF。**下载件一定带票** —— 补票在打开页面的本地服务里做，所以
  **早于本版生成的存量文档也照样带票，不需要重新 publish**

## 锚点约定（联动的命根子）

每段内容前**单独一行**：

```markdown
<!-- pages: 5,6 -->
这段总结的正文……
```

- 页码一律是**原始 PDF 页码**
- **每个内容块都要有自己的锚点**：标题、分隔线不用；
  ⚠️ 常见坑：「引导句 + 空行 + 表格/代码块」是**两个块** —— 表格/代码块前要再补一行锚点，
  否则那段点不了跳转、覆盖率也到不了 100%
- 注释与正文之间空一行；注释本身不会显示
- 页号指向被删页没问题：前端有三段回退（往后最近的保留页 → 往前最近的保留页 → 只高亮不滚动）

## 自检（交付前）

- [ ] `publish` 的锚点覆盖率 = 100%（不是 100% 要说清原因）
- [ ] 每个锚点的页码对得上它标注的那段内容
- [ ] 「此处略过原 PX–PY」提示条出现，且文案与 reason 相符（重复页 = 「与前后页重复」）
- [ ] 把 URL、压缩比、视觉补丁页号报给用户

## 命令卡

| 命令 | 用途 |
|---|---|
| `list` | 看有哪些文档（纯文件操作，**不起服务**） |
| `open <docId>` | 重新打开已有文档（服务没起会自动起，**不重新生成**任何东西） |
| `open <md路径>` | 用户把 `summary.md` 下载到本地后，用**这一条命令**重开并排页面：读文件尾部的回程票拿到 docId。文件名随便改都行（只认注释内容）；没票的 md 会明确报错。从页面下载的 md **一定带票**（存量文档也一样） |
| `stop` | 停服务（服务脱离终端常驻，**关终端窗口杀不掉**） |
| `serve` | 前台跑服务（调试用，Ctrl-C 停） |

> `open` 传 md 路径这一条是「不调用 agent 也能重开」的入口，值得主动告诉用户：
> 下载的 md 不是死文件，随时能凭它原地复活那个并排页面。前提是**同一台机器**
> 且数据还在 `~/.course-digest/`（换机器恢复不了页面，但 md 本身仍是完整总结）。

## 数据在哪

- **用户数据**：`~/.course-digest/`（`docs/<docId>/` 六个产物 + `style.md` 偏好）—— skill 升级永不碰（ADR-014）
- **代码**：本仓库；`course_digest/` 是 CLI 与服务，`course_digest/web/` 是前端，`tools/` 里是视觉补丁用的渲染源码
