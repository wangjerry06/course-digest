# 设计决策（摘要）

这个项目在设计时逐条记了 14 条决策（编号 `ADR-001`…`ADR-014`），代码注释与文档里会引用这些编号。
完整记录（背景、方案对比、后果）保存在维护者本地，未随仓库发布；这里是每条决策的一句话摘要。

## 架构

| 编号 | 决策 | 一句话理由 |
|---|---|---|
| ADR-001 | 本地常驻服务 + 固定 SPA URL | 每次打开文档的成本最低：不需要每次重构页面 |
| ADR-002 | 服务只绑 `127.0.0.1` | 不出本机、不触发防火墙弹窗、不需要任何公网资源 |
| ADR-003 | 数据与前端分离，放弃 `file://` 直开 | 浏览器安全模型下 `file://` 无法做我们要的取数与渲染 |
| ADR-004 | skill 必须自包含，不依赖本机专有工具 | clone 下来断网也能跑；不把环境差异留给用户 |
| ADR-005 | 用 PDF.js 渲染进**自有 canvas** | 联动跳转需要精确控制每一页，内置 viewer 在 iframe 里做不到 |
| ADR-006 | MVP 范围锁定 macOS + OpenClaw | 先在一个平台上真正跑通，Windows 明确不做（代码仍避免平台专有工具） |

## 呈现与交互

| 编号 | 决策 | 一句话理由 |
|---|---|---|
| ADR-007 | 联动只做**单向**点击跳转 | 双向滚动跟随的收益不足以抵掉复杂度，留到下一版 |
| ADR-008 | Markdown 渲染用 KaTeX + 标页码 + **视觉识别按需补丁** | 图密集型课件文本层不够，按需看几页图；看不确定的数值要标注请读者核对原文 |
| ADR-010 | 服务保活：探活 + 按需启动 | 首次使用零配置，之后秒开；启动方式收敛成一个可替换函数 |

## 流程与输出

| 编号 | 决策 | 一句话理由 |
|---|---|---|
| ADR-011 | 核心流程收敛为 **CLI 子命令** | 确定性的事（提文本/压缩/落盘/起服务）交给命令，判断性的事（写总结）留给 agent；换 harness 只需一份 `SKILL.md` |
| ADR-012 | 文本压缩**只做算法单阶段**，不让 LLM 编辑文本 | 写总结本身就是"合并与提炼"，再让模型编辑一遍等于同一件事做两遍，多花约 80% token |
| ADR-013 | 输出**简化版 PDF** + 页码映射 + 「此处略过」提示 | 重复页从 PDF 里也去掉，前端内存从 ≈1.7 GB 降到 ≈300 MB；但删页是有损的 → 必须标出略过范围并能一键切回完整版 |
| ADR-014 | 数据目录与 skill 代码分离（`~/.course-digest/`） | skill 升级/重装会覆盖代码目录，用户的历史文档库不能跟着丢 |

## 发布

| 编号 | 决策 | 一句话理由 |
|---|---|---|
| ADR-009 | MIT + GitHub / ClawHub 发布，命名 `course-digest` | 便于分享与二次使用 |
